"""
Factor-GAN 实验项目的源码包。

这个包只保留三类通用能力：
- `data`：读取你后续自己准备好的月频样本表，并做训练前整理
- `models`：Factor-GAN 的网络结构
- `training`：滚动窗口训练与结果落盘

这里刻意不再保留旧版“因子计算/特征构建”实现，
因为你后续会用自己选定的因子重新完成这一部分。
"""

__all__ = ["data", "models", "training"]
